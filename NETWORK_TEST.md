# 网络与播放测试（2026-09-24）

> 最新变更：网站默认已切换为独立强制海外入口（本次出口检查 US），不会沿用 v2rayN 绕过大陆的规则。最新优化样本：新剧冷首帧约 5.8 秒，前置索引缓存命中切集约 21 毫秒，详见 VALIDATION.md。下面直连/绕过大陆代理对比属于修改前历史数据，不代表当前默认线路。

本机 macOS，xray / v2rayN 监听 SOCKS `127.0.0.1:10808`。用户说明当前绕过大陆地址。下面是**同一台电脑的两种取片路径**，不是部署后大陆与海外观众的双地区验收。

## 真实视频取片

同一个剧集 `7683198247380192281`，每条路径分别取同一视频开头 4 MiB，3 次；随后重新完整下载、解密并生成 H.264/AAC 视频，各测一次。片段请求全部返回 HTTP 206，完整处理返回 HTTP 200。

| 路径 | 片段吞吐量中位数 | 首字节中位数 | 整集下载/解密/兼容处理 |
| --- | ---: | ---: | ---: |
| 显式直连 | 105.62 Mbps | 119.5 ms | 16.92 秒 |
| 当前 v2rayN 配置 | 101.036 Mbps | 128.2 ms | 16.57 秒 |

视频产物约 26.73 MiB、720P、时长约 173 秒。整集测量复用了已解析的播放模型，不含首次模型解析，也不含未来服务器到观众浏览器的一段网络。

独立访问 Cloudflare trace 时，直连返回国家标识 CN，代理返回 US。这个标识**只描述对 Cloudflare 的那一次请求**；不能证明大陆视频 CDN 也走了美国出口。当前代理视频表现与直连接近，但仅凭速度不能确认内部路由。

此前一轮片段采样中位数为 182.532 / 175.344 Mbps；上表是增加整集测试后保留的最终一轮。吞吐量随时间、CPU 与网络负载波动，不是固定线路容量或并发观看承诺。

## 浏览器真实播放

已登录账号、本机网页、本地 MP4 缓存命中；Chrome 实际播放《东北话事人》：

- 首帧：0.029 秒（缓存命中）。
- 实际播放：111.39 秒。
- 播放中等待：0 次 / 0.000 秒。
- 解码丢帧：0 / 3339。

这个样本没有出现卡顿，但只能证明本机缓存播放。完整一集首次准备需要约 17 秒，属于**首播等待**，不是播放途中卡顿。未来部署后，服务器出口、观众到服务器的线路、视频码率和并发都会影响实际体验。

## 复测

```sh
.venv/bin/python scripts/probe_upstream.py --item-id 7683198247380192281 --proxy socks5://127.0.0.1:10808 --prepare-video
```

原始结果：`output/network/upstream-paths.json`、`output/network/browser-local.json`。脚本不会保存视频签名地址、密钥或出口 IP。

播放器已内置「播放体验测试」，可标记网络并保存首帧、准备、播放时长、卡顿与丢帧，管理员在「管理中心」查看。真实大陆/非大陆对比需部署域名后，在对应地区实际打开网站，各播放相同视频至少 60–120 秒。步骤见 [DEPLOYMENT.md](DEPLOYMENT.md)。

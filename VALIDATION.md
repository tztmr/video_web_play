# 验证记录

## 3x-ui 占用 80/443 时的 HTTPS 兼容（2026-09-24）

- 源码改为先探测主机监听端口和 3x-ui 特征文件：不停止 x-ui/xray，不改写 `/root/.acme.sh` 的 `reloadcmd`。443 被占用时复用 `/root/cert/<域名>/`、acme.sh 或 `deploy/certs` 中未过期证书，并把 HTTPS 发布到空闲端口（通常 8443）；仅 80 被占用时改用 TLS-ALPN。独立 acme.sh 安装到 `deploy/acme/`。
- 本机自动化：`.venv/bin/python -m unittest discover -s tests -v` **73/73 通过**。覆盖端口规划、3x-ui 证书复用、未找到证书时 `--copy-cert` 静默退出 2、`deploy/runtime.env` 无引号、占用 80/443 的部署脚本替身路径（探测 `https://域名:8443`、不调用 curl/iptables/systemctl）、安装器读取 `runtime.env`。Caddyfile 兼容模板分别关闭 HTTP-01 或 HTTP/3。
- `bash -n deploy.sh scripts/deploy_stack.sh scripts/reload_caddy.sh` 通过。本机 `docker compose config --quiet` 在默认端口和占用端口（`8443:443` + `Caddyfile.tls`）两种 env 下均通过。
- **证据边界**：以上为源码与本机测试。本机 Docker 守护进程未运行，未做 `caddy validate` 或容器启动。没有连接用户已安装 3x-ui 的服务器，不能证明线上 80/443 冲突、真实 Let's Encrypt 签发或 3x-ui 回落 443 已通过。

## 服务器安装后的读取权限修复（2026-09-24）

- 用户服务器日志确认 `/app/server.py` 返回 `Errno 13 Permission denied`。本地按安装器 `umask 077` 克隆真实仓库，复现 Python 文件为 `0600`、程序目录为 `0700`；旧 Dockerfile 保留这些 root 文件权限，UID 10001 无法启动程序。此前 CI 的普通检出没有覆盖该条件。
- 镜像只调整 `/app`、`/opt/hongguo`、`/opt/geoip` 中程序文件和国家库的读取/目录访问权限，不放宽宿主机配置、备份或 `/data`。构建末尾以真实运行用户导入应用、读取国家库；未启动 lifespan 或生成账号、初始化令牌。
- 部署测试增加容器 unhealthy 分支：返回失败、显示可在任意目录执行的绝对路径日志命令，不执行 HTTPS/初始化，也不删除卷或覆盖已有配置。安装器测试确认宿主机仍保留私有权限。
- Linux CI 改为从 `umask 077` 克隆的受限权限目录构建，启动 UID 10001 容器并检查登录、静态播放模块、真实国家库、代理防伪；额外重启并验证已有数据卷内容保留。具体运行结果以修复提交的 GitHub Actions 为准。
- 没有连接用户的美国服务器；该服务器需要拉取修复并重新构建镜像后验收。

## 浏览器直连取片与解密（2026-09-24，当前默认）

- 完整回归 **62/62 通过**，前端 JS 和部署 Shell 语法检查通过。新增验证：直连接口不下载/转码、匿名分享范围/过期/撤销、解析过程中撤销不再返回密钥、大陆来源无法获取直连描述、HEVC 能力和安全 CDN 范围。
- Node WebCrypto 运行实际 `static/mp4-decrypt.js`，独立生成的双轨加密 MP4 覆盖 `stco/co64`、固定/可变采样大小和多 chunk；与原 Python 解密以及已知明文逐字节一致，损坏结构/越界偏移/无效密钥被拒绝。
- 一条真实 8,225,389 B HEVC 源文件的浏览器算法输出与原 Python 解密逐字节一致。macOS Chrome 实际直连播放《她不再忍让》并自动切集；原生视频 `readyState=4`，播放时间推进；拖动到 60 秒后恢复连续播放。
- 登录播放器的两次切集只向网站请求 792 B / 794 B 播放信息，7,742,435 B / 11,882,197 B 视频由浏览器直接从 `v3-reading-video.qznovelvod.com` 获取。未出现服务器流式视频、媒体缓存或预下载请求；切换兼容模式前，隔离服务的视频缓存文件数为 0。
- 新建浏览器上下文，Cookie 数为 0：有效分享直接播放，源站视频 6,271,264 B，播放描述 792 B，观察时间推进至 106.9 秒。拖动至 30 秒后推进至 31.2 秒，`readyState=4`、无页面脚本错误。390×844 视口页面宽度与内容宽度均为 390，无横向溢出。
- 模拟浏览器不支持 HEVC：只请求 `/api/play/source`，返回 409 并提示手动兼容；模拟分享浏览器源站网络失败：仅获取播放描述，显示直连失败，不请求服务器视频或预下载。恢复网络/能力后，手动兼容模式实际返回视频流并达到 `readyState=4`。
- 验证范围为本机真实 Chrome 和真实视频源站。手机仅做视口检查，未在物理 iOS/Android 上验收；未连接用户美国服务器、核对其出站账单或进行真实跨地区公网测试。此前服务器缓存/转码的速度数据不代表新的浏览器直连速度。

以下为此前功能的验证记录；服务器视频处理和预下载目前仅用于手动选择的兼容模式。

## 菜单部署与大陆 IP 限制（2026-09-24）

- 完整 Python 回归 **55/55 通过**，包含真实 Git 本地仓库的安装/更新流程、失败恢复、工作区保护、保留数据卸载、在线 SQLite WAL 备份、地区访问边界与既有播放回归。安装流程的 Docker 调用在本地单测中使用替身，不代表服务器部署验收。
- 根菜单和内部部署脚本通过 Bash 语法检查，所有前端 JS 通过语法检查，真实 Docker Compose 配置校验通过；检查模式未生成 `.env`。
- 官方 DB-IP Lite 2026-09 地址库下载成功；下载器校验 IPv4/IPv6 元数据与 CN 样本。另有测试保证下载失败、无效压缩数据和无效数据库不会覆盖已有文件。
- 已重启本地 8787 服务并保留账号。在真实 Uvicorn 服务中使用受信任的本机请求模拟来源 IP：大陆 IPv4、IPv6 及映射地址均返回 403；美国、非大陆 IPv6、香港、澳门、台湾样本正常显示登录页，未登录私有接口仍返回 401。未知地区返回 503。本机健康和登录页均为 200。
- 应用用例验证：即使带管理员会话、有效分享链接、Range 头或伪造转发头，大陆来源仍被拒绝；非大陆匿名分享可读取指定视频的 Range 内容，未登录不能读取私有视频。
- GitHub Actions 已加入 Linux 镜像构建、真实数据库检查和实际 Caddy 转发防伪测试；其结果应查看对应提交的 Actions 状态。本机模拟不等同真实大陆/海外网络验收，尚未提供服务器或域名完成公网部署。

## TACO 品牌与独立部署（2026-09-24）

- 首页、登录、分享、管理页面及服务名称已改为 TACO小剧场；本机打开的首页已刷新，浏览器标题、侧栏品牌和排版已核对。
- 运行所需 19 个 Python 文件按固定上游提交纳入 `vendor/hongguo`；SHA-256 清单校验通过，不再依赖相邻下载器目录。
- 本地自动化测试 **39/39 通过**。新增部署配置校验、海外默认与显式直连、特殊字符保留、拒绝 Shell 注入、保留旧项目数据卷、只检查模式及 HTTPS 探测。
- 模拟 Docker 命令的脚本集成用例通过：首次部署、重复更新保留 `.env`、HTTPS 失败返回非零且不打印上线成功。该模拟不代表实际安装 Docker 或申请证书。
- 前端 JavaScript、Bash 语法、真实 `docker compose config --quiet` 通过；`--check` 不创建本地 `.env`、不启动容器。
- 本机 Docker Desktop 已启动，但 Docker Hub 鉴权端点连接超时，镜像拉取未成功。本次补充 GitHub Actions，在 Linux 上构建镜像、启动生产配置容器、检查登录边界和 Caddy 配置；实际运行结果以仓库 Actions 为准。
- GitHub 首次构建 [35958514561](https://github.com/tztmr/video_web_play/actions/runs/35958514561) 对应提交 `295b0bb`。GitHub 在任何步骤开始前拒绝任务，备注为账号近期付款失败或需要提高消费限额；未获得 Linux 镜像构建/容器启动结果。这是平台执行限制，不能记为构建通过。
- 尚无目标服务器或域名，未执行公网部署、证书签发和跨地区播放验收。


## 二次播放提速（2026-09-24，兼容模式历史记录）

- **30/30 测试通过**，包括原有登录/分享权限，新增长任务复用、前台抢占预加载、排队取消后重试、分段范围/长度异常回退、取消全部分段请求、播放模型并发合并，以及真实 FFmpeg 缓存索引回归。
- 真实冷启动阶段拆分：同一 720p 集，旧路径播放地址解析 1,446 ms，单路下载 2,354 ms，解密 378 ms，首段输出 259 ms。第二个样本下载 2,582 ms、解密 796 ms。等待主要仍在地址解析和完整取片。
- 同一 6,271,264 字节视频做交替下载：单连接 2,479/482 ms，4 段 2,129/328 ms，4 次 SHA-256 完全一致。冷/热连接差异明显，因此不是固定提速百分比。
- 本轮真实浏览器首次打开《她不再忍让》第 1 集（720p，无视频缓存）：首帧 **5,823 ms**，服务器响应中的 model/download/decrypt/first_output 分别 1,392.3 / 2,160.4 / 402.1 / 128.7 ms；总流请求 21,793 ms。之前 8,644 ms 是上一轮样本，网络时点不同，不作为严格 A/B 比例。
- 当前集播放并完整生成缓存后，前端自动准备第 2 集；临时目录确认下一集已完成。最多一个后台预加载，不提前下载整部短剧；用户切到其他剧集时优先前台任务，切入同一预加载任务可从已有首段读取。
- 实测发现 fragmented MP4 缓存导致 Chrome 为媒体信息反复发出上百次 Range 请求：同一第 2 集缓存开播耗时 **4,302 ms**。改为复制音视频流、生成前置索引后，从点击第 2 集到该集新 loadedmetadata 后的 playing **21 ms**，currentTime=0、readyState=4，首帧前媒体请求为 **1 次**。这是本机缓存/预加载命中样本，不是海外冷下载速度。
- 真实 FFmpeg 回归确认前置 moov、无 moof，转换前后解码视频帧 MD5 相同、时长一致；缓存处理不降低画质。原有缓存会在启动、接受请求前补齐索引，避免活动 Range 请求的偏移改变。
- 匿名浏览器（0 Cookie）打开永久分享并切至第 2 集，readyState=4，媒体路径使用 `/shared-media/`；权限和有效期校验仍然生效。
- 正式 8787 服务已重启，health 200；未登录首页 303、预加载接口 401；原有 30 个视频缓存均补齐索引，未改账号和分享数据。隔离测试账号与缓存已清理。
- Chrome 控制台 0 errors / 0 warnings。JS 语法、Compose 配置均通过；仍未做远程部署或真实跨地区客户端验证。
- 截图：`output/playwright/speed-indexed-cache.png`、`speed-shared.png`；原始分段/阶段数据：`output/network/range-comparison.json`、`stages-before.json`。

## 流式播放、海外默认线路与分享天数（历史记录）

2026-09-24：

- 自动化测试 **22/22 通过**：新增默认永久链接、0 天/2 天/无效天数、匿名共享缓存播放、共享流接口范围与撤销、流式完成后发布缓存、首块提前输出、关闭后释放上游与名额、缓存读取不等待其他转码、强制代理规则不修改原配置。
- `.venv` 隔离实例 8788 真实浏览器播放《她不再忍让》第 1 集（720p）：点击后 `loadedmetadata` 8,440 ms、`canplay/playing` 8,644 ms，流请求完成 17,940 ms；使用 blob MediaSource 播放，观察时 readyState=4，进度推进至 42.628 秒，采集期间无 waiting/error 事件。这是本机一次样本，含目录请求，不是跨地区 SLA 或严格与旧版的 A/B 测试。
- 网站专用 Xray 配置强制将本地入口流量路由到当前 `proxy` 节点，不带 v2rayN 的大陆直连规则；同一专用入口 Cloudflare 查询国家为 US。原 v2rayN 配置未修改，账号/节点密钥未写入项目。
- 真实匿名冷流接口首块 4.41 秒返回；中途关闭后重试同一集，1.14 秒返回首块，未卡住转码名额。这是首块时间，不是首帧时间。
- 桌面实测分享区位于视频下面；默认值 0，创建后显示“永久有效”；单独无 Cookie 浏览器打开链接，readyState=4，没有进入登录页。
- UI 设置 2 天，返回到期时间约 48 小时后。手机 390×844 无横向溢出；主播放器和管理中心已移除播放体验测试界面。
- 浏览器控制台 0 errors / 0 warnings；JS 语法、Compose 配置检查通过。生产 Docker 镜像和异地服务器尚未实测。
- 截图：`output/playwright/stream-share-desktop.png`、`stream-share-mobile.png`、`stream-shared-guest.png`。

下面是早期版本的历史记录；其中完整准备后播放、播放器测速 UI 已由本轮修改替代。

## 登录、分享与上线准备更新

2026-09-24 本轮新增验证：

- `python3 -m unittest discover -s tests -v`：**16/16 通过**，包括初始化令牌一次性、密码加盐哈希、会话 Cookie、默认登录、普通账号不能管理、停用/改密撤销会话、限流、分享范围、到期/撤销、已缓存媒体的权限及测速记录访问权限。
- 正式 8787 入口重启后：`/` → 303 `/login`，未登录目录 API 和已有 MP4 均返回 401；登录页与健康检查正常。
- 隔离测试实例 8788 使用临时账号完成实际表单登录；在管理页面实际创建并停用观众，生成/撤销分享。
- 新建无 Cookie 浏览器上下文，分享视频实际 `readyState=4`，播放时间推进；普通视频地址 401，越权剧集 403；后台撤销后链接 410。
- 在登录用户播放器保存并在后台显示真实测试：缓存命中连续播放 111.393 秒，0 次卡顿、0/3339 丢帧，首帧 28.8 ms。
- 浏览器控制台：0 errors / 0 warnings（另有密码表单可访问性 verbose 提示）。
- `node --check` 通过；`DOMAIN=cinema.example.com docker compose config --quiet` 通过。
- 本机 Docker 守护进程未运行；没有执行容器构建、HTTPS 证书签发或远程部署。代理/直连整集与片段结果见 [NETWORK_TEST.md](NETWORK_TEST.md)。
- 测试账号与初始化校验使用隔离临时数据，不是正式管理员账号。正式账号由用户通过一次性初始化页自行创建。

新增截图：`output/playwright/login.png`、`admin.png`、`share-created.png`、`shared-guest.png`。

## 初始版本记录

日期：2026-09-24（Asia/Shanghai）。环境：本机 macOS、Python 3、FFmpeg、Chrome。

## 真实 API

通过运行中的 `http://127.0.0.1:8787` 验证，未使用演示数据：

| 检查 | 结果 |
| --- | --- |
| 本地服务、播放组件 | health 200，playback_ready=true |
| 真人剧、漫剧、AI 剧场分类 | 三者均 200，首批各 24 部 |
| 真人短剧搜索「东北话事人」 | 200，10 条，有后续页 |
| 漫剧搜索「系统」 | 200，10 条，有后续页 |
| 真人剧榜单第 1、2 页 | 各 20 条，两页 ID 无交集 |
| 《东北话事人》目录 | 71 集 |
| 《东北话事人》第 1 集 | MP4 / H.264 + AAC / 720×1280 / 173.081 秒 |
| 《东北话事人》第 2 集 | MP4 / H.264 + AAC / 1080×1920 / 170.551 秒 |
| 漫剧播放准备 | 200，正常生成兼容 MP4 |

## 实际浏览器

- 桌面页显示真实海报、题材和剧集数。
- 真人剧首集 `readyState=4`，`paused=false`，播放时间实际推进。
- 拖到 45 秒，关闭播放器后重开，恢复到至少 44 秒处。
- 首集播放到末尾，自动进入第 2 集；后续视频完成准备并记录观看进度。
- 观看视频时服务返回 `206 Partial Content`。
- 手机尺寸 390×844：页面和播放器均无横向溢出。
- 手机布局点击漫剧得到 24 部，搜索得到 10 部，收藏列表显示已收藏项目。
- 手机布局点击榜单后显示 20 部，继续加载后显示 40 部。
- 手机播放器观看《重生！丑小鸭逆袭成了万人迷第二季》：目录 250 集，实际视频 `readyState=4`，播放到 24.65 秒，总长 120.976 秒，画面 1280×720。
- 浏览器控制台检查：0 errors、0 warnings。

截图在 `output/playwright/`：`home-desktop.png`、`player-desktop.png`、`home-mobile.png`、`player-mobile.png`。

## 自动化

`python3 -m unittest discover -s tests -v`：6/6 通过。

覆盖缓存复用与实际清晰度、Range / 206 / 416、同集并发去重、失败不写缓存、取消释放名额、文件清理与本地请求边界。`node --check static/app.js` 通过。

## 验证边界

上述播放为真实上游和本机 Chrome 的验证，手机尺寸使用浏览器调整视口，未在物理手机或其他操作系统上测试。AI 剧场已验证真实片单，未逐部验证播放。原下载器源码作为运行依赖保留在相邻目录；未修改、打包或发布原下载器。
